---
phase: 01-mobile-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/Input.tsx
  - src/components/layout/Navbar.tsx
  - src/components/ui/ThemeToggle.tsx
  - src/components/rating/LikeButton.tsx
  - src/components/user/FollowButton.tsx
  - src/components/rating/CommentsSection.tsx
autonomous: true
requirements: [MOB-01, MOB-06]

must_haves:
  truths:
    - "Every interactive element in the navbar has at least 44px tappable area"
    - "ThemeToggle button has at least 44px tappable area"
    - "LikeButton has at least 44px touch target height"
    - "FollowButton has at least 44px touch target height"
    - "Comment trigger button has at least 44px touch target height"
    - "Comment sheet close button has at least 44px tappable area"
    - "All text inputs use 16px font-size (no iOS Safari auto-zoom)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Fixed .input class font-size"
      contains: "font-size: 16px"
    - path: "src/components/ui/Input.tsx"
      provides: "Fixed Input component font-size"
      contains: "text-base"
    - path: "src/components/layout/Navbar.tsx"
      provides: "44px touch targets on search and notification icons"
      contains: "min-w-11 min-h-11"
    - path: "src/components/ui/ThemeToggle.tsx"
      provides: "44px touch target"
      contains: "44px"
    - path: "src/components/rating/LikeButton.tsx"
      provides: "44px minimum touch target height"
      contains: "minHeight"
    - path: "src/components/rating/CommentsSection.tsx"
      provides: "44px touch targets on trigger and close buttons"
      contains: "minHeight"
  key_links:
    - from: "src/app/globals.css"
      to: "all components using .input class"
      via: "CSS class application"
      pattern: "font-size: 16px"
    - from: "src/components/ui/Input.tsx"
      to: "Settings page, RatingForm"
      via: "component import"
      pattern: "text-base"
---

<objective>
Fix all touch targets and input font sizes across the app to meet mobile usability standards.

Purpose: Ensure every interactive element has a minimum 44px tappable area (per Apple HIG) and all text inputs use 16px font-size to prevent iOS Safari auto-zoom. These are foundational fixes that affect the entire mobile experience.

Output: Seven files updated with correct touch target sizes and input font sizes.
</objective>

<execution_context>
@C:/Users/teose/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/teose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mobile-ux/01-RESEARCH.md
@.planning/phases/01-mobile-ux/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix global input font-size and Input component</name>
  <files>src/app/globals.css, src/components/ui/Input.tsx</files>
  <read_first>
    - src/app/globals.css (current .input class at line 414, font-size: 15px)
    - src/components/ui/Input.tsx (current text-sm class on line 25)
  </read_first>
  <action>
In `src/app/globals.css`, change the `.input` class `font-size` from `15px` to `16px` (line 421).

In `src/components/ui/Input.tsx`, change the `text-sm` class to `text-base` on the input element (line 25). The class string currently reads:
```
`w-full px-4 py-2.5 rounded-lg bg-[#141414] border ... text-sm outline-none ...`
```
Change `text-sm` to `text-base`. Do not change any other classes or styles.

Why: iOS Safari auto-zooms when an input has font-size below 16px. `text-base` = 16px in Tailwind. The `.input` class at 15px triggers the same issue on textarea elements using it (e.g., Settings bio, RatingForm review text).
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && grep -n "font-size: 16px" src/app/globals.css | grep ".input" || grep -n "font-size: 16px" src/app/globals.css | head -5 && grep -n "text-base" src/components/ui/Input.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `font-size: 16px;` inside the `.input` block (was `15px`)
    - `src/components/ui/Input.tsx` contains `text-base` (was `text-sm`) in the input className
    - No other changes to either file
  </acceptance_criteria>
  <done>`.input` class uses 16px font-size; Input.tsx component uses text-base (16px); iOS Safari will not auto-zoom on any input focus</done>
</task>

<task type="auto">
  <name>Task 2: Fix Navbar and ThemeToggle touch targets</name>
  <files>src/components/layout/Navbar.tsx, src/components/ui/ThemeToggle.tsx</files>
  <read_first>
    - src/components/layout/Navbar.tsx (search icon line 59-68, notification icon line 71-81, avatar/menu button line 88-105)
    - src/components/ui/ThemeToggle.tsx (button style width: 36px, height: 36px on lines 13-14)
  </read_first>
  <action>
**Navbar.tsx — Search icon (line 62):**
Change `className="sm:hidden w-9 h-9 flex items-center justify-center rounded-full ..."` to `className="sm:hidden min-w-11 min-h-11 flex items-center justify-center rounded-full ..."`. Keep the SVG icon size (width="19" height="19") unchanged. Only the container gets the larger hit area.

**Navbar.tsx — Notifications icon (line 74):**
Same change: replace `w-9 h-9` with `min-w-11 min-h-11` in the className. Keep SVG size unchanged.

**Navbar.tsx — Avatar/menu button (line 90):**
Add `min-h-11` to the button className. Current: `className="flex items-center gap-2 py-1 px-2 rounded-lg ..."`. Change to: `className="flex items-center gap-2 py-1 px-2 rounded-lg min-h-11 ..."`. The visual avatar circle (w-8 h-8) stays the same inside.

**ThemeToggle.tsx — Button (line 12-14):**
Change `width: '36px'` to `width: '44px'` and `height: '36px'` to `height: '44px'` in the button's style object. The emoji inside will naturally center due to the existing flexbox alignment.

Why: All four elements are currently 36px or smaller, below the 44px minimum touch target for mobile. `min-w-11` = 44px in Tailwind. We use `min-w`/`min-h` instead of `w`/`h` to set a floor without preventing growth.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && grep -n "min-w-11 min-h-11" src/components/layout/Navbar.tsx && grep -n "min-h-11" src/components/layout/Navbar.tsx && grep -n "44px" src/components/ui/ThemeToggle.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/Navbar.tsx` contains `min-w-11 min-h-11` on the search icon Link (was `w-9 h-9`)
    - `src/components/layout/Navbar.tsx` contains `min-w-11 min-h-11` on the notifications icon Link (was `w-9 h-9`)
    - `src/components/layout/Navbar.tsx` contains `min-h-11` on the avatar/menu button
    - `src/components/ui/ThemeToggle.tsx` style object contains `width: '44px'` and `height: '44px'` (was `36px`)
    - SVG icon sizes in Navbar.tsx remain `width="19" height="19"` (visual size unchanged)
    - ThemeToggle emoji display unchanged
  </acceptance_criteria>
  <done>Navbar search, notification, and avatar buttons all have 44px minimum touch targets; ThemeToggle is 44x44px; all visual icon sizes unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Fix LikeButton, FollowButton, and CommentsSection touch targets</name>
  <files>src/components/rating/LikeButton.tsx, src/components/user/FollowButton.tsx, src/components/rating/CommentsSection.tsx</files>
  <read_first>
    - src/components/rating/LikeButton.tsx (button style padding: '5px 12px' on line 85)
    - src/components/user/FollowButton.tsx (button style padding: '9px 24px' on line 54)
    - src/components/rating/CommentsSection.tsx (trigger button padding: '10px 16px' on line 323, close button padding: '4px' on line 174)
  </read_first>
  <action>
**LikeButton.tsx (line 83-93):**
Add `minHeight: '44px'` to the button style object. Change padding from `'5px 12px'` to `'10px 12px'`. The button already has `display: 'inline-flex'` and `alignItems: 'center'`, so the content will center within the taller target. The existing inline style object on lines 83-93 should have these two changes:
- Line 85: `padding: '10px 12px'` (was `'5px 12px'`)
- Add: `minHeight: '44px'` (new property, add after `padding`)

**FollowButton.tsx (line 53-54):**
Add `minHeight: '44px'` to the button style object. The current `padding: '9px 24px'` with 14px font and line-height gives roughly 18px content + 18px padding = ~36px. Adding minHeight ensures it meets 44px. Add the property on a new line after line 54.

**CommentsSection.tsx — Trigger button (lines 321-328):**
Add `minHeight: '44px'` to the trigger button's style object. Current padding is `'10px 16px'` which should be close but needs the explicit minimum. Add it to the style object on line 323.

**CommentsSection.tsx — Close button (lines 166-181):**
Change `padding: '4px'` to `padding: '10px'` and add `minWidth: '44px', minHeight: '44px'` to the close button style. The SVG icon stays 18x18px. This gives a 44px hit area around the X icon.

**CommentsSection.tsx — Comment input (lines 265-274):**
Change the comment input `fontSize: '13px'` to `fontSize: '16px'` to prevent iOS zoom. This is the input on line 272 inside the bottom sheet form.

Why: LikeButton at 5px vertical padding is only ~22px tall. FollowButton is ~36px. Comment close button at 4px padding is ~26px. All below the 44px minimum. Comment input at 13px triggers iOS zoom.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && grep -n "minHeight" src/components/rating/LikeButton.tsx && grep -n "minHeight" src/components/user/FollowButton.tsx && grep -n "minHeight" src/components/rating/CommentsSection.tsx && grep -n "fontSize: '16px'" src/components/rating/CommentsSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/rating/LikeButton.tsx` button style contains `minHeight: '44px'` and `padding: '10px 12px'` (was `'5px 12px'`)
    - `src/components/user/FollowButton.tsx` button style contains `minHeight: '44px'`
    - `src/components/rating/CommentsSection.tsx` trigger button style contains `minHeight: '44px'`
    - `src/components/rating/CommentsSection.tsx` close button style contains `minWidth: '44px'` and `minHeight: '44px'` and `padding: '10px'` (was `'4px'`)
    - `src/components/rating/CommentsSection.tsx` comment input contains `fontSize: '16px'` (was `'13px'`)
    - No visual design changes (colors, fonts, icons unchanged)
  </acceptance_criteria>
  <done>LikeButton, FollowButton, comment trigger, and comment close all have 44px minimum touch targets; comment input uses 16px font-size to prevent iOS zoom</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `grep -rn "font-size: 15px" src/app/globals.css` returns NO results (15px eliminated from .input)
2. `grep -rn "text-sm" src/components/ui/Input.tsx` returns NO results (text-sm eliminated)
3. `grep -rn "w-9 h-9" src/components/layout/Navbar.tsx` returns NO results (36px icons eliminated)
4. `grep -rn "width: '36px'" src/components/ui/ThemeToggle.tsx` returns NO results (36px eliminated)
5. `grep -rn "padding: '5px 12px'" src/components/rating/LikeButton.tsx` returns NO results (5px eliminated)
6. All modified files pass TypeScript compilation: `npx tsc --noEmit`
</verification>

<success_criteria>
- All input elements use 16px font-size (prevents iOS Safari auto-zoom)
- All interactive elements have minimum 44px touch targets
- No visual design changes (same icon sizes, same colors, same visual layout)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-ux/01-01-SUMMARY.md`
</output>
