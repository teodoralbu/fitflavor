---
phase: 04-quality-accessibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/loading.tsx
  - src/app/flavors/[slug]/loading.tsx
  - src/app/products/[slug]/loading.tsx
  - src/app/browse/loading.tsx
  - src/app/leaderboard/loading.tsx
  - src/app/users/[username]/loading.tsx
  - src/app/search/loading.tsx
  - src/app/page.tsx
  - src/app/leaderboard/page.tsx
  - src/app/users/[username]/page.tsx
  - src/app/search/page.tsx
  - src/app/globals.css
autonomous: true
requirements: [UX-04, UX-05]

must_haves:
  truths:
    - "User sees skeleton loading states while any data-fetching page loads"
    - "User sees meaningful empty states when feed, search, profile, or leaderboard has no data"
    - "Empty states have descriptive text and action links where appropriate"
    - "Typography weights are consolidated to 400/700 per UI-SPEC"
  artifacts:
    - path: "src/app/loading.tsx"
      provides: "Home page loading skeleton"
      min_lines: 10
    - path: "src/app/flavors/[slug]/loading.tsx"
      provides: "Flavor detail loading skeleton"
      min_lines: 8
    - path: "src/app/products/[slug]/loading.tsx"
      provides: "Product detail loading skeleton"
      min_lines: 8
    - path: "src/app/browse/loading.tsx"
      provides: "Browse page loading skeleton"
      min_lines: 8
    - path: "src/app/leaderboard/loading.tsx"
      provides: "Leaderboard loading skeleton"
      min_lines: 8
    - path: "src/app/users/[username]/loading.tsx"
      provides: "User profile loading skeleton"
      min_lines: 8
    - path: "src/app/search/loading.tsx"
      provides: "Search page loading skeleton"
      min_lines: 8
  key_links:
    - from: "src/app/loading.tsx"
      to: "src/app/globals.css"
      via: ".skeleton CSS class"
      pattern: "className.*skeleton"
---

<objective>
Create loading.tsx skeleton files for all data-fetching routes and add consistent empty states across the app.

Purpose: UX-05 requires loading states during data fetches. UX-04 requires graceful empty states. UI-SPEC mandates specific skeleton structures and empty state copy.
Output: 7 loading.tsx files, updated empty states in feed/search/profile/leaderboard, typography weight fixes.
</objective>

<execution_context>
@C:/Users/teose/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/teose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-accessibility/04-RESEARCH.md
@.planning/phases/04-quality-accessibility/04-UI-SPEC.md

<interfaces>
<!-- Existing .skeleton class in globals.css (line 384) -->
.skeleton {
  background: linear-gradient(90deg, var(--bg-card) 25%, var(--bg-elevated) 50%, var(--bg-card) 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}

<!-- CSS custom properties available -->
--radius-md, --radius-lg for border-radius
--space-md (16px), --space-xl (24px), --space-2xl (32px) for padding/gaps

<!-- Typography weight updates needed per UI-SPEC -->
.m-section-title: change font-weight from 800 to 700
.m-segment-tab: change font-weight from 600 to 700
.badge (line ~347): change font-weight from 600 to 700
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loading.tsx skeleton files for all data-fetching routes</name>
  <files>src/app/loading.tsx, src/app/flavors/[slug]/loading.tsx, src/app/products/[slug]/loading.tsx, src/app/browse/loading.tsx, src/app/leaderboard/loading.tsx, src/app/users/[username]/loading.tsx, src/app/search/loading.tsx</files>
  <read_first>
    - src/app/globals.css (verify .skeleton class exists and understand available CSS vars)
    - src/app/page.tsx (understand home page layout structure for matching skeleton)
    - src/app/flavors/[slug]/page.tsx (understand flavor detail layout for skeleton)
    - src/app/products/[slug]/page.tsx (understand product detail layout for skeleton)
    - src/app/browse/page.tsx (understand browse layout for skeleton)
    - src/app/leaderboard/page.tsx (understand leaderboard layout for skeleton)
    - src/app/users/[username]/page.tsx (understand profile layout for skeleton)
    - src/app/search/page.tsx (understand search layout for skeleton)
  </read_first>
  <action>
Create 7 `loading.tsx` files. Each is a default export React component using the existing `.skeleton` CSS class. All skeletons use inline styles with CSS custom properties. No imports needed beyond React (implicit in Next.js).

**src/app/loading.tsx** (home page):
```tsx
export default function Loading() {
  return (
    <div style={{ padding: '20px 16px', maxWidth: 600, margin: '0 auto' }}>
      <div className="skeleton" style={{ height: 32, width: 200, marginBottom: 24, borderRadius: 'var(--radius-md)' }} />
      <div style={{ display: 'flex', gap: 12, marginBottom: 24 }}>
        {[1, 2, 3, 4].map(i => (
          <div key={i} className="skeleton" style={{ height: 64, flex: 1, borderRadius: 'var(--radius-md)' }} />
        ))}
      </div>
      {[1, 2, 3].map(i => (
        <div key={i} className="skeleton" style={{ height: 140, width: '100%', marginBottom: 12, borderRadius: 'var(--radius-lg)' }} />
      ))}
    </div>
  )
}
```

**src/app/flavors/[slug]/loading.tsx** (flavor detail):
```tsx
export default function Loading() {
  return (
    <div style={{ padding: '20px 16px', maxWidth: 600, margin: '0 auto' }}>
      <div className="skeleton" style={{ height: 24, width: 240, marginBottom: 16, borderRadius: 'var(--radius-md)' }} />
      <div className="skeleton" style={{ height: 200, width: '100%', marginBottom: 16, borderRadius: 'var(--radius-lg)' }} />
      <div className="skeleton" style={{ height: 16, width: '80%', marginBottom: 8, borderRadius: 'var(--radius-md)' }} />
      <div className="skeleton" style={{ height: 16, width: '60%', marginBottom: 24, borderRadius: 'var(--radius-md)' }} />
    </div>
  )
}
```

**src/app/products/[slug]/loading.tsx** (product detail):
```tsx
export default function Loading() {
  return (
    <div style={{ padding: '20px 16px', maxWidth: 600, margin: '0 auto' }}>
      <div className="skeleton" style={{ height: 24, width: 200, marginBottom: 16, borderRadius: 'var(--radius-md)' }} />
      <div className="skeleton" style={{ height: 180, width: '100%', marginBottom: 16, borderRadius: 'var(--radius-lg)' }} />
      <div className="skeleton" style={{ height: 16, width: '90%', marginBottom: 8, borderRadius: 'var(--radius-md)' }} />
      <div className="skeleton" style={{ height: 16, width: '70%', marginBottom: 8, borderRadius: 'var(--radius-md)' }} />
      <div className="skeleton" style={{ height: 16, width: '50%', borderRadius: 'var(--radius-md)' }} />
    </div>
  )
}
```

**src/app/browse/loading.tsx** (browse page):
```tsx
export default function Loading() {
  return (
    <div style={{ padding: '20px 16px', maxWidth: 600, margin: '0 auto' }}>
      <div className="skeleton" style={{ height: 24, width: 160, marginBottom: 16, borderRadius: 'var(--radius-md)' }} />
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 12 }}>
        {[1, 2, 3, 4, 5, 6].map(i => (
          <div key={i} className="skeleton" style={{ height: 120, borderRadius: 'var(--radius-lg)' }} />
        ))}
      </div>
    </div>
  )
}
```

**src/app/leaderboard/loading.tsx** (leaderboard):
```tsx
export default function Loading() {
  return (
    <div style={{ padding: '20px 16px', maxWidth: 600, margin: '0 auto' }}>
      <div className="skeleton" style={{ height: 24, width: 180, marginBottom: 16, borderRadius: 'var(--radius-md)' }} />
      {[1, 2, 3, 4, 5].map(i => (
        <div key={i} className="skeleton" style={{ height: 72, width: '100%', marginBottom: 8, borderRadius: 'var(--radius-md)' }} />
      ))}
    </div>
  )
}
```

**src/app/users/[username]/loading.tsx** (user profile):
```tsx
export default function Loading() {
  return (
    <div style={{ padding: '20px 16px', maxWidth: 600, margin: '0 auto', display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
      <div className="skeleton" style={{ height: 64, width: 64, borderRadius: '50%', marginBottom: 12 }} />
      <div className="skeleton" style={{ height: 20, width: 140, marginBottom: 8, borderRadius: 'var(--radius-md)' }} />
      <div className="skeleton" style={{ height: 16, width: 200, marginBottom: 24, borderRadius: 'var(--radius-md)' }} />
      {[1, 2, 3].map(i => (
        <div key={i} className="skeleton" style={{ height: 120, width: '100%', marginBottom: 12, borderRadius: 'var(--radius-lg)' }} />
      ))}
    </div>
  )
}
```

**src/app/search/loading.tsx** (search results):
```tsx
export default function Loading() {
  return (
    <div style={{ padding: '20px 16px', maxWidth: 600, margin: '0 auto' }}>
      <div className="skeleton" style={{ height: 48, width: '100%', marginBottom: 16, borderRadius: 'var(--radius-md)' }} />
      {[1, 2, 3, 4].map(i => (
        <div key={i} className="skeleton" style={{ height: 72, width: '100%', marginBottom: 8, borderRadius: 'var(--radius-md)' }} />
      ))}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && ls src/app/loading.tsx src/app/flavors/\[slug\]/loading.tsx src/app/products/\[slug\]/loading.tsx src/app/browse/loading.tsx src/app/leaderboard/loading.tsx src/app/users/\[username\]/loading.tsx src/app/search/loading.tsx 2>&1 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/app/loading.tsx` succeeds (file exists)
    - `ls src/app/flavors/\[slug\]/loading.tsx` succeeds
    - `ls src/app/products/\[slug\]/loading.tsx` succeeds
    - `ls src/app/browse/loading.tsx` succeeds
    - `ls src/app/leaderboard/loading.tsx` succeeds
    - `ls src/app/users/\[username\]/loading.tsx` succeeds
    - `ls src/app/search/loading.tsx` succeeds
    - Each file contains `className="skeleton"` (uses existing CSS class)
    - Each file exports a default function named `Loading`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All 7 loading.tsx files exist, use the .skeleton CSS class, and TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add consistent empty states and fix typography weights</name>
  <files>src/app/page.tsx, src/app/leaderboard/page.tsx, src/app/users/[username]/page.tsx, src/app/search/page.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/page.tsx (find existing empty state text around line 522, understand feed tab structure for global/following)
    - src/app/leaderboard/page.tsx (find "No ratings yet" around line 198)
    - src/app/users/[username]/page.tsx (find "No ratings yet" around lines 256, 591)
    - src/app/search/page.tsx (find "Nothing found" around line 101)
    - src/app/globals.css (find .m-section-title at ~line 192, .m-segment-tab at ~line 233, .badge at ~line 347)
    - .planning/phases/04-quality-accessibility/04-UI-SPEC.md (Copywriting Contract section for exact empty state copy)
  </read_first>
  <action>
**Part A — Empty states (UX-04):**

Update existing empty state messages to match the UI-SPEC Copywriting Contract. Each empty state should be a centered container with a heading, body text, and optional action link. Use inline styles consistent with the project.

Empty state pattern to apply (not a separate component — inline in each page):
```tsx
<div style={{ textAlign: 'center', padding: '48px 16px' }}>
  <p style={{ fontSize: '18px', fontWeight: 700, color: 'var(--text)', marginBottom: 8 }}>{heading}</p>
  <p style={{ fontSize: '14px', color: 'var(--text-muted)', marginBottom: 16 }}>{body}</p>
  {action && <a href={action.href} style={{ color: 'var(--accent)', fontSize: '14px', fontWeight: 700 }}>{action.label}</a>}
</div>
```

**src/app/page.tsx — Home feed empty states:**
- Global tab empty: heading "No reviews yet", body "Be the first to rate a flavor and share your opinion.", action link "Rate a flavor" href="/rate"
- Following tab (logged in) empty: heading "Nothing here yet", body "Follow people to see their reviews in this feed.", action link "Browse flavors" href="/browse"
- Following tab (logged out) empty: heading "Log in to see your feed", body "Sign in to follow people and see their reviews here.", action link "Log in" href="/login"
- Find the existing empty state rendering around line 522 and replace with the pattern above. The following tab needs auth-awareness — check if user is available in the component's scope to choose between logged-in and logged-out variants.

**src/app/leaderboard/page.tsx** (~line 198):
- Replace "No ratings yet" with: heading "No ratings yet", body "Flavors will appear here once they receive ratings.", no action link.

**src/app/users/[username]/page.tsx** (~lines 256, 591):
- Replace "No ratings yet." with: heading "No ratings yet", body "This user has not rated any flavors yet.", no action link.

**src/app/search/page.tsx** (~line 101):
- When total === 0, replace "Nothing found" with: heading "No results found", body "Try a different search term or browse all flavors.", action link "Browse all" href="/browse".

**Part B — Typography weight fixes (UI-SPEC):**

In `src/app/globals.css`:
- Line ~193: Change `.m-section-title { font-weight: 800 }` to `font-weight: 700`
- Line ~193: Change `.m-section-title { font-size: 17px }` to `font-size: 18px`
- Line ~234: Change `.m-segment-tab { font-weight: 600 }` to `font-weight: 700`
- Line ~348: Change `.badge { font-weight: 600 }` to `font-weight: 700` (if this is the correct badge class — verify during read)
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && grep -c "No reviews yet" src/app/page.tsx && grep -c "Nothing here yet" src/app/page.tsx && grep -c "No results found" src/app/search/page.tsx && grep "font-weight: 700" src/app/globals.css | head -5 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "No reviews yet" src/app/page.tsx` finds the global feed empty state heading
    - `grep "Nothing here yet" src/app/page.tsx` finds the following tab logged-in empty state
    - `grep "Log in to see your feed" src/app/page.tsx` finds the following tab logged-out empty state
    - `grep "No results found" src/app/search/page.tsx` finds the search empty state
    - `grep "No ratings yet" src/app/leaderboard/page.tsx` still present as heading text
    - `grep "This user has not rated" src/app/users/\[username\]/page.tsx` finds profile empty state
    - `grep "font-weight: 800" src/app/globals.css` returns 0 matches (all 800 replaced with 700)
    - `grep "font-size: 17px" src/app/globals.css` returns 0 matches in .m-section-title (changed to 18px)
    - `.m-segment-tab` has `font-weight: 700` (not 600)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All data pages show descriptive empty states with action links per UI-SPEC copy. Typography weights consolidated to 400/700. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- All 7 loading.tsx files exist and export default Loading components using .skeleton class
- Empty states match UI-SPEC Copywriting Contract exactly
- `npx next build` completes successfully
- Typography uses only weights 400 and 700 in globals.css class definitions
</verification>

<success_criteria>
- Every data-fetching route has a loading.tsx showing skeletons during fetch
- Every page that can be empty shows descriptive text + action link (where applicable)
- Feed empty states are auth-aware (different copy for logged in vs logged out on following tab)
- Typography weights match UI-SPEC (700 for all emphasis, no 600 or 800)
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-accessibility/04-02-SUMMARY.md`
</output>
