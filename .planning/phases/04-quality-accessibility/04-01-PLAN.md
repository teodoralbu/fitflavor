---
phase: 04-quality-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries.ts
  - src/context/auth-context.tsx
  - src/components/rating/RatingForm.tsx
  - src/components/rating/CommentsSection.tsx
  - src/components/rating/LikeButton.tsx
  - src/app/rate/[slug]/page.tsx
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "queries.ts has zero `as any` casts and all Supabase calls check `.error`"
    - "auth-context.tsx, RatingForm.tsx, CommentsSection.tsx, LikeButton.tsx, rate/[slug]/page.tsx have zero `as any` casts"
    - "TypeScript compiles without errors after removing all casts"
    - "Avatar upload validation exists (QUAL-03 already implemented in Phase 2)"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "Typed Supabase queries with error handling"
      contains: "if (error)"
    - path: "src/context/auth-context.tsx"
      provides: "Typed auth context Supabase calls"
    - path: "src/components/rating/RatingForm.tsx"
      provides: "Typed rating form Supabase calls"
    - path: "src/components/rating/CommentsSection.tsx"
      provides: "Typed comments Supabase calls"
    - path: "src/components/rating/LikeButton.tsx"
      provides: "Typed like Supabase calls"
    - path: "src/app/rate/[slug]/page.tsx"
      provides: "Typed rate page Supabase calls"
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/lib/supabase-server.ts"
      via: "createServerSupabaseClient() returning SupabaseClient<Database>"
      pattern: "createServerSupabaseClient"
    - from: "src/lib/queries.ts"
      to: "src/lib/types.ts"
      via: "Database type generic on client"
      pattern: "Database"
---

<objective>
Remove all `as any` casts from critical-path files and add consistent error handling to every Supabase query in queries.ts.

Purpose: QUAL-01 requires type-safe critical paths. QUAL-02 requires error handling in data-fetching. QUAL-03 was already implemented in Phase 2 and just needs verification.
Output: Type-safe queries.ts, typed client components, `npx tsc --noEmit` passes clean.
</objective>

<execution_context>
@C:/Users/teose/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/teose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-accessibility/04-RESEARCH.md

<interfaces>
<!-- From src/lib/supabase-server.ts — the client is already typed -->
createServerSupabaseClient() returns SupabaseClient<Database>

<!-- From src/lib/supabase.ts — browser client -->
createClient() returns SupabaseClient<Database>

<!-- From src/lib/types.ts — Database type exists with Tables definition -->
The Database type is already passed as generic to both createServerClient<Database>() and createBrowserClient<Database>().
The `as any` pattern exists because every query function reassigns: `const db = supabase as any`.
Fix: use `supabase` directly instead of creating `db`.

<!-- Critical detail from research -->
Join selects like `select('*, brands(*)')` may not infer correctly without Relationships metadata.
If join types fail, use `.returns<T>()` or explicit type annotations on the result.
Do NOT generate Supabase types — use manual annotations if needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove as-any casts and add error handling in queries.ts</name>
  <files>src/lib/queries.ts, src/lib/types.ts</files>
  <read_first>
    - src/lib/queries.ts (full file — understand all 7 query functions and their `as any` patterns)
    - src/lib/types.ts (Database type definition — understand table/column types available)
    - src/lib/supabase-server.ts (verify createServerSupabaseClient returns typed client)
  </read_first>
  <action>
In `src/lib/queries.ts`, apply two changes to every function:

**Change 1 — Remove `as any`:**
- Delete every `const db = supabase as any` line. Use `supabase` directly for all `.from()` calls.
- Delete every `as any[]` cast on query results. Instead, type the destructured data properly.
- For inline casts like `((flavors ?? []) as any[]).map(...)`, remove the cast. If TypeScript cannot infer the type from the Supabase select, add an explicit type annotation on the destructured variable: `const { data: flavors }: { data: FlavorRow[] | null } = await supabase...` or use `.returns<T[]>()`.
- For join results like `f.flavor_tag_assignments`, if Supabase cannot infer the nested type, define a local interface at the top of queries.ts:
  ```typescript
  interface FlavorWithTags {
    id: string
    name: string
    slug: string
    product_id: string
    image_url: string | null
    flavor_tag_assignments: { flavor_tags: { id: string; name: string; slug: string } }[]
  }
  ```
  Then use `.returns<FlavorWithTags[]>()` on the query.
- For the `Record<string, any[]>` grouped ratings pattern, replace with `Record<string, { flavor_id: string; overall_score: number; would_buy_again: boolean }[]>`.

**Change 2 — Add error handling (QUAL-02):**
- On every Supabase call, destructure `error` alongside `data`: `const { data, error } = await supabase...`
- After each call, add: `if (error) { console.error('[functionName] query failed:', error.message); return []; }` (or `return null` for single-item functions like `getProductBySlug`, `getFlavorBySlug`).
- Functions that return arrays (`getLeaderboard`, `getGlobalFeed`, `getFollowingFeed`, `getFeedForUser`): return `[]` on error.
- Functions that return objects or null (`getProductBySlug`, `getFlavorBySlug`): return `null` on error.

**Verification step:** After all changes, run `npx tsc --noEmit`. If type errors appear on join selects, use `.returns<T>()` to annotate those specific queries rather than reverting to `as any`. Add explicit interface definitions in queries.ts for any complex join shapes that TypeScript cannot infer.

**QUAL-03 verification:** Confirm `src/components/user/AvatarUpload.tsx` already contains MIME validation (jpeg/png/webp allowlist) and 5MB size check. No code changes needed — just verify during read_first that the validation exists at the top of the upload handler.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" src/lib/queries.ts` returns 0
    - `grep -c "if (error)" src/lib/queries.ts` returns at least 7 (one per Supabase call minimum)
    - `grep -c "console.error" src/lib/queries.ts` returns at least 7
    - `npx tsc --noEmit` exits with code 0
    - `grep "allowedTypes\|MIME\|jpeg.*png.*webp\|5.*MB\|5242880" src/components/user/AvatarUpload.tsx` returns at least 1 match (QUAL-03 verification)
  </acceptance_criteria>
  <done>queries.ts has zero `as any` casts, every Supabase call checks `.error` with console.error logging, TypeScript compiles clean, and QUAL-03 avatar validation confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Remove as-any casts from client components and rate page</name>
  <files>src/context/auth-context.tsx, src/components/rating/RatingForm.tsx, src/components/rating/CommentsSection.tsx, src/components/rating/LikeButton.tsx, src/app/rate/[slug]/page.tsx</files>
  <read_first>
    - src/context/auth-context.tsx (find the `as any` cast on line 27 and understand query context)
    - src/components/rating/RatingForm.tsx (find `as any` on line 159 and understand the Supabase call)
    - src/components/rating/CommentsSection.tsx (find `as any` on line 43 and line 72, understand queries)
    - src/components/rating/LikeButton.tsx (find `as any` on line 27, understand the query)
    - src/app/rate/[slug]/page.tsx (find `as any` on line 16 and line 36, understand queries)
    - src/lib/types.ts (understand available types for annotations)
    - src/lib/supabase.ts (verify createClient returns typed client)
  </read_first>
  <action>
For each file, remove `as any` casts by using the already-typed Supabase client directly:

**src/context/auth-context.tsx (line 27):**
- Remove `const db = supabase as any`. Use `supabase` directly for the `.from()` call.
- The query fetches user profile data. Type should resolve from Database generic.
- If the select string has joins that don't resolve, use `.returns<{ id: string; username: string; avatar_url: string | null; ... }>()`.

**src/components/rating/RatingForm.tsx (line 159):**
- Remove `const db = supabase as any`. Use `supabase` directly.
- This is inside `handleSubmit` — likely an insert or select. Use the typed client.
- Add `{ error }` destructuring and check: `if (error) { console.error('[RatingForm] query failed:', error.message); return; }`.

**src/components/rating/CommentsSection.tsx (line 43, line 72):**
- Remove `const db = useMemo(() => createClient() as any, [])`. Replace with `const db = useMemo(() => createClient(), [])`.
- Remove `as any[]` cast on line 72 for user mapping. Type the data properly from Supabase response.
- Add error checks on Supabase calls.

**src/components/rating/LikeButton.tsx (line 27):**
- Remove `const db = useMemo(() => createClient() as any, [])`. Replace with `const db = useMemo(() => createClient(), [])`.
- Add error checks on Supabase calls.

**src/app/rate/[slug]/page.tsx (line 16, line 36):**
- Remove `const db = supabase as any`. Use `supabase` directly.
- Remove `as any[]` cast on line 36. Let types flow from Database generic.
- If join selects don't resolve, use `.returns<T>()` with explicit interface.

After all changes, run `npx tsc --noEmit`. Fix any remaining type errors using `.returns<T>()` or explicit type annotations — never revert to `as any`.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as any" src/context/auth-context.tsx` returns 0
    - `grep -c "as any" src/components/rating/RatingForm.tsx` returns 0
    - `grep -c "as any" src/components/rating/CommentsSection.tsx` returns 0
    - `grep -c "as any" src/components/rating/LikeButton.tsx` returns 0
    - `grep -c "as any" src/app/rate/\[slug\]/page.tsx` returns 0
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All 5 client-side critical-path files have zero `as any` casts and TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -r "as any" src/lib/queries.ts src/context/auth-context.tsx src/components/rating/RatingForm.tsx src/components/rating/CommentsSection.tsx src/components/rating/LikeButton.tsx src/app/rate/\[slug\]/page.tsx` returns zero matches
- `npx next build` completes successfully
</verification>

<success_criteria>
- Zero `as any` casts remain in the 6 critical-path files
- Every Supabase call in queries.ts destructures and checks `.error`
- TypeScript strict mode compilation passes
- QUAL-03 avatar validation confirmed present (no new code needed)
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-accessibility/04-01-SUMMARY.md`
</output>
