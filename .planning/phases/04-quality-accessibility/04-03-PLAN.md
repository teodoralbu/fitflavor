---
phase: 04-quality-accessibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/user/AvatarUpload.tsx
  - src/components/feed/FeedCard.tsx
  - src/components/rating/ReviewCard.tsx
  - src/components/rating/CommentsSection.tsx
  - src/components/rating/RatingForm.tsx
  - src/components/layout/Navbar.tsx
  - src/app/browse/page.tsx
  - src/app/flavors/[slug]/page.tsx
  - src/app/products/[slug]/page.tsx
  - src/app/users/[username]/page.tsx
  - src/app/leaderboard/page.tsx
  - src/app/settings/page.tsx
  - src/app/rep/page.tsx
  - src/app/notifications/page.tsx
  - src/app/rate/RateSearch.tsx
  - src/app/rate/RateLanding.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03]

must_haves:
  truths:
    - "User can tab through all interactive elements and see a visible focus ring"
    - "All product/flavor/avatar images have descriptive alt text"
    - "Decorative SVG icons have aria-hidden=true"
    - "Color contrast meets WCAG AA for all readable text"
    - "--text-faint is only used for placeholder/disabled elements, never readable content"
  artifacts:
    - path: "src/app/globals.css"
      provides: "WCAG AA compliant color tokens"
      contains: "--text-dim"
    - path: "src/components/user/AvatarUpload.tsx"
      provides: "Accessible avatar upload with role=button and keyboard support"
      contains: "role=\"button\""
  key_links:
    - from: "src/components/user/AvatarUpload.tsx"
      to: "src/app/globals.css"
      via: ":focus-visible global rule"
      pattern: "tabIndex"
    - from: "all image components"
      to: "alt text"
      via: "alt prop on img/NextImage"
      pattern: "alt="
---

<objective>
Make all interactive elements keyboard-accessible with visible focus, add alt text to all images, and fix color contrast to meet WCAG AA.

Purpose: UX-01 requires keyboard focus visibility. UX-02 requires image alt text. UX-03 requires WCAG AA contrast compliance.
Output: Accessible interactive elements, descriptive alt text on all images, contrast-compliant color tokens.
</objective>

<execution_context>
@C:/Users/teose/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/teose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-accessibility/04-RESEARCH.md
@.planning/phases/04-quality-accessibility/04-UI-SPEC.md

<interfaces>
<!-- Global focus-visible rule in globals.css (line 155) -->
:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}

<!-- Button component already has focus-visible ring (from Tailwind) -->
<!-- .input class has outline: none but compensates with border-color + box-shadow on :focus -->

<!-- Current color tokens that need contrast fixes (dark theme) -->
--text-dim: #556070  (needs lightening to #6B7A90 if used on --bg-card: #161B22)
--text-faint: #374050  (acceptable ONLY for placeholder/disabled)

<!-- Current color tokens (light theme) -->
--text-dim: #7A8090  (verify passes 4.5:1 on #FFFFFF, darken to #6A7080 if not)
--text-faint: #B0B8C8  (acceptable ONLY for placeholder/disabled)

<!-- AvatarUpload interactive div (line 84) -->
onClick={() => fileInputRef.current?.click()}
Missing: role="button", tabIndex={0}, onKeyDown, aria-label

<!-- Files with alt= occurrences (23 total across 16 files) -->
Many images lack alt text or have empty alt=""
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix contrast tokens, focus states, and interactive div accessibility</name>
  <files>src/app/globals.css, src/components/user/AvatarUpload.tsx</files>
  <read_first>
    - src/app/globals.css (full file — understand all color token declarations in :root and [data-theme="light"], find all uses of --text-faint for non-placeholder content)
    - src/components/user/AvatarUpload.tsx (full file — find the clickable div on line 84 and understand the component structure)
    - .planning/phases/04-quality-accessibility/04-UI-SPEC.md (Color section and Accessibility Contract for exact fixes)
  </read_first>
  <action>
**Part A — WCAG AA contrast fixes in globals.css (UX-03):**

Dark theme `:root` section:
- Change `--text-dim: #556070` to `--text-dim: #6B7A90` (ensures 4.5:1 on --bg-card: #161B22)
- Keep `--text-faint: #374050` as-is (only used for placeholder/disabled per UI-SPEC)

Light theme `[data-theme="light"]` section:
- Change `--text-dim: #7A8090` to `--text-dim: #6A7080` (ensures 4.5:1 on --bg-card: #FFFFFF)
- Keep `--text-faint: #B0B8C8` as-is (only used for placeholder/disabled per UI-SPEC)

**Part B — Audit --text-faint usage for non-placeholder content:**

Search all files for `text-faint` usage. The following are ACCEPTABLE uses (placeholder/disabled/decorative):
- `globals.css` `.input::placeholder` and `.input-search::placeholder` — OK
- `globals.css` scrollbar thumb — OK (decorative)
- `globals.css` border-color rules — OK (decorative)
- `CommentsSection.tsx` "No comments yet" text — OK (empty state)
- `CommentsSection.tsx` timestamp text — this is readable content, should use `--text-muted` instead
- `ReviewCard.tsx` timestamp/meta text — should use `--text-muted` instead if it's readable
- `BottomNav.tsx` inactive icon color — OK (icon state indicator, not text)
- `Footer.tsx` footer text — should use `--text-muted` if it's meant to be readable
- `brands/[slug]/page.tsx` breadcrumb separators — OK (decorative)
- `brands/[slug]/page.tsx` "Unrated" label and rating count — should use `--text-dim` or `--text-muted`
- `flavors/[slug]/page.tsx` breadcrumb separator — OK (decorative)

For each non-acceptable use of `--text-faint` on readable content, change to `var(--text-muted)` or `var(--text-dim)`. Specifically:
- `CommentsSection.tsx` line ~229: change timestamp `color: 'var(--text-faint)'` to `color: 'var(--text-muted)'`
- `ReviewCard.tsx` line ~102: if this is readable text, change `color: 'var(--text-faint)'` to `color: 'var(--text-muted)'`
- `Footer.tsx` lines ~47, ~121, ~130: change `color: 'var(--text-faint)'` to `color: 'var(--text-muted)'`
- `brands/[slug]/page.tsx` lines ~133, ~136: change rating count and "Unrated" from `var(--text-faint)` to `var(--text-dim)`

**Part C — AvatarUpload keyboard accessibility (UX-01):**

In `src/components/user/AvatarUpload.tsx`, find the `<div>` with `onClick={() => fileInputRef.current?.click()}` (line ~84). Add these attributes:
```tsx
<div
  role="button"
  tabIndex={0}
  onClick={() => fileInputRef.current?.click()}
  onKeyDown={(e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      e.preventDefault()
      fileInputRef.current?.click()
    }
  }}
  aria-label="Upload avatar photo"
  style={{ cursor: 'pointer', /* ...existing styles */ }}
>
```

**Part D — Verify range slider focus visibility:**

Check `src/app/globals.css` for `input[type="range"]` styles. The `:focus-visible` global rule should apply to the range input itself. If there's an `outline: none` on the range input, remove it or ensure the focus state is visible through an alternative (e.g., box-shadow on the track). The existing `.input` compensation pattern (border-color change) does not apply to range sliders.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && grep "text-dim.*#6B7A90" src/app/globals.css && grep "text-dim.*#6A7080" src/app/globals.css && grep 'role="button"' src/components/user/AvatarUpload.tsx && grep 'tabIndex' src/components/user/AvatarUpload.tsx && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "#6B7A90" src/app/globals.css` matches (dark theme --text-dim updated)
    - `grep "#6A7080" src/app/globals.css` matches (light theme --text-dim updated)
    - `grep 'role="button"' src/components/user/AvatarUpload.tsx` matches
    - `grep 'tabIndex={0}' src/components/user/AvatarUpload.tsx` matches
    - `grep 'onKeyDown' src/components/user/AvatarUpload.tsx` matches
    - `grep 'aria-label="Upload avatar photo"' src/components/user/AvatarUpload.tsx` matches
    - `grep "text-faint" src/components/rating/CommentsSection.tsx` returns 0 or only for the "No comments" empty state text (line 204, acceptable)
    - `grep "text-faint" src/components/layout/Footer.tsx` returns 0 matches (all changed to --text-muted)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Color contrast tokens meet WCAG AA, --text-faint restricted to placeholder/disabled only, AvatarUpload is keyboard-accessible with role/tabIndex/onKeyDown/aria-label.</done>
</task>

<task type="auto">
  <name>Task 2: Add alt text to all images across the app</name>
  <files>src/components/feed/FeedCard.tsx, src/components/rating/ReviewCard.tsx, src/components/rating/CommentsSection.tsx, src/components/rating/RatingForm.tsx, src/components/layout/Navbar.tsx, src/components/user/AvatarUpload.tsx, src/app/browse/page.tsx, src/app/flavors/[slug]/page.tsx, src/app/products/[slug]/page.tsx, src/app/users/[username]/page.tsx, src/app/leaderboard/page.tsx, src/app/settings/page.tsx, src/app/rep/page.tsx, src/app/notifications/page.tsx, src/app/rate/RateSearch.tsx, src/app/rate/RateLanding.tsx</files>
  <read_first>
    - src/components/feed/FeedCard.tsx (find all img/NextImage tags and their current alt props)
    - src/components/rating/ReviewCard.tsx (find all img/NextImage tags)
    - src/components/rating/CommentsSection.tsx (find all img tags)
    - src/components/rating/RatingForm.tsx (find img tags)
    - src/components/layout/Navbar.tsx (find img tags)
    - src/app/browse/page.tsx (find img/NextImage tags)
    - src/app/flavors/[slug]/page.tsx (find img/NextImage tags)
    - src/app/products/[slug]/page.tsx (find img/NextImage tags)
    - src/app/users/[username]/page.tsx (find img/NextImage tags)
    - src/app/leaderboard/page.tsx (find img/NextImage tags)
    - src/app/settings/page.tsx (find img tags)
    - src/app/rep/page.tsx (find img tags)
    - src/app/notifications/page.tsx (find img tags)
    - src/app/rate/RateSearch.tsx (find img tags)
    - src/app/rate/RateLanding.tsx (find img tags)
    - .planning/phases/04-quality-accessibility/04-UI-SPEC.md (Image Alt Text section for exact patterns)
  </read_first>
  <action>
Audit every `<img>`, `<NextImage>` (aliased as `Image` or `NextImage`), and `<svg>` tag across all listed files. Apply alt text per the UI-SPEC patterns:

**Alt text rules:**
- Product images: `alt="{product name} by {brand name}"` — use the variable names available in scope (e.g., `product.name`, `brand.name`)
- Flavor images: `alt="{flavor name} - {product name}"` — use `flavor.name`, `product.name` variables
- User avatars: `alt="{username}'s avatar"` — use `user.username` or equivalent variable
- Brand logos: `alt="{brand name} logo"` — use `brand.name` variable
- Decorative SVG icons (hamburger, close, arrows, stars): add `aria-hidden="true"` to the `<svg>` element

**For each file:**
1. Find all image elements (`<img`, `<Image`, `<NextImage`)
2. Check if `alt` prop exists and is descriptive
3. If `alt=""` (empty) on an informational image, add descriptive text using the patterns above
4. If `alt` is missing entirely on `<img>`, add it
5. NextImage already requires `alt` prop (TypeScript enforces), but check that the value is descriptive not just `alt=""`
6. Find all `<svg>` elements that are decorative icons and add `aria-hidden="true"` if not present

**Common patterns to look for and fix:**
- `alt=""` on product/flavor images → change to descriptive
- `alt="avatar"` or `alt="profile"` → change to `alt={`${username}'s avatar`}`
- Missing `aria-hidden="true"` on icon SVGs (search, heart, comment, star icons)

Do NOT modify SVGs that convey meaning (e.g., a star rating that is the only indicator of rating value). Those need `aria-label` instead.
  </action>
  <verify>
    <automated>cd "C:/Users/teose/gymtaste app/gymtaste" && grep -r 'alt=""' src/components/ src/app/ --include="*.tsx" | grep -v node_modules | head -20 && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r 'alt=""' src/components/ src/app/ --include="*.tsx"` returns 0 matches for informational images (decorative images with alt="" are acceptable only if they also have role="presentation")
    - `grep -c "aria-hidden" src/components/feed/FeedCard.tsx` returns at least 1 (SVG icons marked)
    - `grep "alt=" src/components/feed/FeedCard.tsx` shows descriptive alt text (not empty strings) for product/flavor/avatar images
    - `grep "alt=" src/app/browse/page.tsx` shows descriptive alt text for product images
    - `grep "'s avatar" src/components/feed/FeedCard.tsx` matches (avatar images have username-based alt)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All informational images have descriptive alt text following UI-SPEC patterns. All decorative SVGs have aria-hidden="true". TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Color contrast tokens updated in globals.css for both themes
- AvatarUpload div has role="button", tabIndex={0}, onKeyDown, aria-label
- No informational images have empty alt text
- `npx next build` completes successfully
</verification>

<success_criteria>
- All interactive elements reachable and visible via keyboard Tab navigation
- Every product, flavor, and avatar image has descriptive alt text
- Decorative SVGs have aria-hidden="true"
- --text-dim passes WCAG AA 4.5:1 in both themes
- --text-faint restricted to placeholder/disabled elements only
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-accessibility/04-03-SUMMARY.md`
</output>
