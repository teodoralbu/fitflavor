---
phase: 03-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/components/feed/FeedCard.tsx
  - src/components/user/AvatarUpload.tsx
  - src/components/rating/RatingForm.tsx
  - src/components/rating/CommentsSection.tsx
  - src/components/rating/ReviewCard.tsx
  - src/app/browse/page.tsx
  - src/app/notifications/page.tsx
  - src/app/users/[username]/page.tsx
  - src/app/leaderboard/page.tsx
  - src/app/rep/page.tsx
autonomous: true
requirements:
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "All images in the app use next/image instead of raw <img> tags"
    - "Images load with correct width/height to prevent layout shift"
    - "Supabase storage images are served through Next.js image optimization"
    - "No eslint-disable @next/next/no-img-element comments remain"
  artifacts:
    - path: "next.config.ts"
      provides: "Image remote patterns for Supabase"
      contains: "remotePatterns"
    - path: "src/components/feed/FeedCard.tsx"
      provides: "Feed cards with next/image"
      contains: "import Image from"
  key_links:
    - from: "next.config.ts"
      to: "Supabase storage"
      via: "remotePatterns hostname"
      pattern: "jzgkjwjjpymjnznkktaq\\.supabase\\.co"
    - from: "src/components/feed/FeedCard.tsx"
      to: "next/image"
      via: "Image component import"
      pattern: "import Image from .next/image."
---

<objective>
Replace all raw `<img>` tags with `next/image` across the entire app and configure Next.js image optimization for Supabase storage.

Purpose: Every image currently uses raw `<img>` with no width/height, causing layout shift (CLS) and missing Next.js automatic optimization (WebP/AVIF conversion, responsive sizing, lazy loading). The `next.config.ts` has zero image configuration.

Output: `next.config.ts` with Supabase remote patterns, all 10 files with `<img>` converted to `next/image`.
</objective>

<execution_context>
@C:/Users/teose/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/teose/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-performance/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure next.config.ts and convert FeedCard + core component images</name>
  <files>next.config.ts, src/components/feed/FeedCard.tsx, src/components/rating/CommentsSection.tsx, src/components/rating/ReviewCard.tsx, src/components/rating/RatingForm.tsx</files>
  <read_first>
    - next.config.ts (currently empty config)
    - src/components/feed/FeedCard.tsx (4 img tags: avatar x2, product thumbnail, review photo)
    - src/components/rating/CommentsSection.tsx (img tags for comment avatars)
    - src/components/rating/ReviewCard.tsx (img tags for review display)
    - src/components/rating/RatingForm.tsx (img tags in form)
  </read_first>
  <action>
**Step 1: Configure next.config.ts**

Add the `images` configuration with the exact Supabase project hostname:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'jzgkjwjjpymjnznkktaq.supabase.co',
        pathname: '/storage/v1/object/public/**',
      },
    ],
  },
};

export default nextConfig;
```

**Step 2: Convert FeedCard.tsx (4 `<img>` tags)**

Add `import Image from 'next/image'` at the top.

Replace each `<img>` with `<Image>` using these exact mappings:

1. **Avatar in RepCard** (line ~143): 32x32 avatar
   - From: `<img src={item.user.avatar_url} alt="" loading="lazy" decoding="async" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />`
   - To: `<Image src={item.user.avatar_url} alt="" width={32} height={32} className="rounded-full" style={{ objectFit: 'cover' }} />`

2. **Progress photo in RepCard** (line ~167): Full-width photo
   - From: `<img src={item.photo_url} alt="Progress photo" style={{ width: '100%', borderRadius: '10px', maxHeight: '340px', objectFit: 'cover', display: 'block' }} />`
   - To: `<Image src={item.photo_url} alt="Progress photo" width={400} height={340} style={{ width: '100%', height: 'auto', maxHeight: '340px', borderRadius: '10px', objectFit: 'cover', display: 'block' }} />`

3. **Avatar in rating card** (line ~264): 32x32 avatar
   - Same pattern as #1 but for `ratingData.user.avatar_url`

4. **Product thumbnail** (line ~308): 52x52 product image
   - From: `<img src={product.image_url} alt="" loading="lazy" decoding="async" style={{ width: '100%', height: '100%', objectFit: 'contain', padding: '4px' }} />`
   - To: `<Image src={product.image_url} alt="" width={52} height={52} style={{ objectFit: 'contain', padding: '4px' }} />`

5. **Review photo** (line ~372): Full-width review photo
   - From: `<img src={ratingData.photo_url} alt="Review photo" loading="lazy" decoding="async" style={{ width: '100%', borderRadius: '10px', maxHeight: '220px', objectFit: 'cover', display: 'block' }} />`
   - To: `<Image src={ratingData.photo_url} alt="Review photo" width={400} height={220} style={{ width: '100%', height: 'auto', maxHeight: '220px', borderRadius: '10px', objectFit: 'cover', display: 'block' }} />`

Remove ALL `// eslint-disable-next-line @next/next/no-img-element` comments from FeedCard.tsx.

**Step 3: Convert CommentsSection.tsx**
Read the file, find all `<img>` tags (likely comment author avatars), replace with `<Image>` using width={24} height={24} for small avatars or width={32} height={32} for standard avatars (match current size). Add `import Image from 'next/image'`. Remove eslint-disable comments.

**Step 4: Convert ReviewCard.tsx**
Read the file, find all `<img>` tags, replace with `<Image>`. Avatar images: width/height matching current CSS size. Review photos: width={400} height={220} with `style={{ width: '100%', height: 'auto' }}`. Add import, remove eslint-disable comments.

**Step 5: Convert RatingForm.tsx**
Read the file, find all `<img>` tags (likely image preview in form), replace with `<Image>`. For upload previews that use blob URLs (data: or blob:), keep as `<img>` since next/image cannot optimize local blobs -- only convert remote Supabase URLs. Remove eslint-disable comments for converted images only.

IMPORTANT: For any image where the `src` comes from a local blob/data URL (file upload preview), keep it as `<img>` -- next/image only works with remote URLs or static imports. Check the src origin before converting.
  </action>
  <verify>
    <automated>cd "/c/Users/teose/gymtaste app/gymtaste" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep for `remotePatterns` in next.config.ts returns 1 match
    - grep for `jzgkjwjjpymjnznkktaq.supabase.co` in next.config.ts returns 1 match
    - grep for `import Image from` in src/components/feed/FeedCard.tsx returns 1 match
    - grep for `no-img-element` in src/components/feed/FeedCard.tsx returns 0 matches
    - grep for `<Image` in src/components/feed/FeedCard.tsx returns at least 4 matches
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>next.config.ts has Supabase image remote patterns. FeedCard.tsx, CommentsSection.tsx, ReviewCard.tsx, and RatingForm.tsx use next/image for all remote images. No eslint-disable no-img-element comments remain in converted files. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Convert remaining page-level images to next/image</name>
  <files>src/app/browse/page.tsx, src/app/notifications/page.tsx, src/app/users/[username]/page.tsx, src/app/leaderboard/page.tsx, src/app/rep/page.tsx, src/components/user/AvatarUpload.tsx</files>
  <read_first>
    - src/app/browse/page.tsx (product listing images)
    - src/app/notifications/page.tsx (notification avatars)
    - src/app/users/[username]/page.tsx (profile page images)
    - src/app/leaderboard/page.tsx (leaderboard item images)
    - src/app/rep/page.tsx (rep page images)
    - src/components/user/AvatarUpload.tsx (avatar preview -- likely uses blob URLs)
  </read_first>
  <action>
For each of the 6 files, follow this procedure:

1. Read the file
2. Find all `<img>` tags
3. For each `<img>`:
   a. If `src` is a remote URL (Supabase storage, avatar_url, image_url) -- convert to `<Image>` with explicit width/height
   b. If `src` is a local blob/data URL (file upload preview, `URL.createObjectURL`) -- keep as `<img>` (next/image cannot optimize these)
4. Add `import Image from 'next/image'` if any conversions were made
5. Remove `// eslint-disable-next-line @next/next/no-img-element` comments for converted images

**Size mapping guide:**
- Avatars: Use the pixel size from the parent container's CSS (commonly 32x32, 40x40, 48x48, or 64x64)
- Product thumbnails: Match container size (commonly 48x48, 52x52, or 64x64)
- Large photos: width={400} height={300} with `style={{ width: '100%', height: 'auto' }}`

**AvatarUpload.tsx special case:** This component has both a preview of the uploaded file (blob URL -- keep as `<img>`) and possibly a display of the current avatar (remote URL -- convert to `<Image>`). Read carefully to distinguish.

**For server components** (browse/page.tsx, leaderboard/page.tsx etc.): `next/image` works in both server and client components, so conversion is straightforward.

After converting all files, do a final sweep:
```bash
grep -rn "no-img-element" src/ --include="*.tsx"
```
Any remaining eslint-disable comments for no-img-element should only exist next to blob/data URL images that legitimately cannot use next/image.
  </action>
  <verify>
    <automated>cd "/c/Users/teose/gymtaste app/gymtaste" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep for `no-img-element` across all .tsx files returns at most 1-2 matches (only blob URL previews)
    - grep for `import Image from` in src/app/browse/page.tsx returns 1 match (if it had img tags)
    - grep for `import Image from` in src/app/users/[username]/page.tsx returns 1 match
    - grep for `import Image from` in src/app/leaderboard/page.tsx returns 1 match
    - `npm run build` completes successfully (catches any missing remotePatterns or invalid Image props)
  </acceptance_criteria>
  <done>All remote images across the entire app use next/image. Only local blob URL previews (if any) remain as raw img tags. Full build passes, confirming all remote patterns are configured correctly.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (critical -- this validates remotePatterns config)
- `grep -rn "no-img-element" src/ --include="*.tsx"` returns at most 1-2 results (blob URLs only)
- `grep -rn "<img " src/ --include="*.tsx"` returns at most 1-2 results (blob URLs only)
- All images have explicit width/height props preventing CLS
</verification>

<success_criteria>
- next.config.ts configured with Supabase hostname in remotePatterns
- All 10 files with raw img tags converted to next/image (except blob URL previews)
- Every Image component has explicit width and height props
- Full build passes
- No eslint-disable no-img-element comments remain for remote images
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance/03-02-SUMMARY.md`
</output>
